# Relational AI 4 Nursing - Hugging Face Space Requirements
#
# IMPORTANT: Do NOT add gradio here. For gradio-SDK Spaces, Hugging Face
# installs gradio[oauth]==<sdk_version from README.md front-matter> itself;
# a second gradio pin in this file makes the build fail with
# ResolutionImpossible (this is exactly what broke the Space build when
# Dependabot added gradio>=6 here).
huggingface_hub>=0.30
torch>=2.13.0
transformers>=5.13.0
accelerate>=1.14.0
bitsandbytes>=0.49.2
peft>=0.10.0
