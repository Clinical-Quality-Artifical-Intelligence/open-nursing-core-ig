# Relational AI 4 Nursing - Hugging Face Space Requirements
#
# ERA-LOCKED STACK — read before bumping anything here.
# The Space runs gradio SDK 4.44.x (installed by HF from the sdk_version in
# README.md front-matter; NEVER pin gradio in this file). The stack below is
# pinned to the era gradio 4.44 shipped against, because the modern majors
# are mutually exclusive with it:
#   - huggingface_hub 1.x removed HfFolder (gradio 4.44's oauth imports it)
#   - transformers 5.x requires hub 1.x
#   - starlette >=0.39 changed TemplateResponse's signature; gradio 4.44
#     calls it positionally -> "TypeError: unhashable type: 'dict'" on every
#     request, and gradio's startup self-check then dies with the misleading
#     "localhost is not accessible" error
#   - pydantic >=2.10 breaks gradio 4.x API schema generation
# Resolver-verified (py3.11, exact Space install incl. gradio[oauth]==4.44.1
# + spaces): gradio 4.44.1 / fastapi 0.112.4 / starlette 0.38.6 /
# pydantic 2.9.2 / hub 0.36.2 / transformers 4.x. Dependabot ignores
# semver-major updates here — do NOT merge Dependabot PRs that re-raise
# these majors (that is what broke the Space three times).
huggingface_hub>=0.30,<1.0
fastapi>=0.110,<0.113
starlette<0.39
pydantic>=2.6,<2.10
# Backport of the stdlib audioop module removed in Python 3.13 (PEP 594);
# gradio 4.x -> pydub needs it. No-op on the pinned 3.11 runtime, protective
# if the python_version pin is ever lifted.
audioop-lts>=0.2.1; python_version >= "3.13"
torch>=2.4
transformers>=4.45,<5
accelerate>=0.30
bitsandbytes>=0.43
peft>=0.19.1
