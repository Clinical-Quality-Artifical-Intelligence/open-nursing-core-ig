# Relational AI 4 Nursing - Hugging Face Space Requirements
#
# ERA-LOCKED STACK — read before bumping anything here.
# The Space runs gradio SDK 4.44.0 (installed by HF from the sdk_version in
# README.md front-matter; NEVER pin gradio in this file). gradio 4.44 imports
# HfFolder, which huggingface_hub 1.0 removed — and transformers 5.x requires
# hub 1.x — so the modern majors are mutually exclusive with this SDK.
# Verified with pip's resolver (py3.11): this set solves; hub>=1 or
# transformers>=5 makes it ResolutionImpossible or crashes at import.
# Dependabot is configured to ignore semver-major updates in this directory.
huggingface_hub>=0.30,<1.0
# Backport of the stdlib audioop module removed in Python 3.13 (PEP 594);
# gradio 4.x -> pydub needs it. No-op on the pinned 3.11 runtime, protective
# if the python_version pin is ever lifted.
audioop-lts>=0.2.1; python_version >= "3.13"
torch>=2.4
transformers>=4.45,<5
accelerate>=1.14.0
bitsandbytes>=0.43
peft>=0.10.0
